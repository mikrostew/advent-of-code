use run_aoc::runner_fn;

#[runner_fn]
fn part1(file_contents: String) -> usize {
    println!("{}", file_contents);
    0
}

#[runner_fn]
fn part2(file_contents: String) -> usize {
    println!("{}", file_contents);
    0
}

#[cfg(test)]
mod tests {
    // use run_aoc::test_fn;

    // test_fn!(day23, part1, example, 0);
    // test_fn!(day23, part1, input, 0);

    // test_fn!(day23, part2, example, 0);
    // test_fn!(day23, part2, input, 0);
}
