use std::fmt::Debug;
use std::fs;
use std::path::Path;

super::run_parts!();

fn part1<P: AsRef<Path> + Debug>(path: P) -> () {
    println!("File {:?}", path);
    unimplemented!();
}

fn part2<P: AsRef<Path> + Debug>(path: P) -> () {
    println!("File {:?}", path);
    unimplemented!();
}
